{
  "$schema": "http://json-schema.org/draft-07/schema",
  "$id": "https://stellar-oracle.io/schemas/cargo-check.json",
  "title": "Cargo Check",
  "description": "Check Rust code using Cargo",
  "type": "object",
  "properties": {
    "cwd": {
      "type": "string",
      "description": "Working directory for cargo command"
    },
    "allTargets": {
      "type": "boolean",
      "description": "Check all targets (bins, tests, examples)",
      "default": false
    },
    "allFeatures": {
      "type": "boolean",
      "description": "Enable all features",
      "default": false
    },
    "verbose": {
      "type": "boolean",
      "description": "Verbose output",
      "default": false
    }
  },
  "required": ["cwd"],
  "additionalProperties": false
}
